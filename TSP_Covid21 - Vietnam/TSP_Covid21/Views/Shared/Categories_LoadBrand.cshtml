<!--
    ➠ Load danh sách thương hiệu ➧ thuộc loại sản phẩm được chọn   ✔

    ➠ Hiện tại chưa có form dành cho danh sách yêu thích của user (nơi chứa tạm cho danh sách yêu thíc)

    ❦ Nơi sử dụng:
        ☞ Form Categories ➧ thuộc Control Covid21

    ☯ Key Session
        ➛ Session["ProductTypeName"]
-->

@model IEnumerable<TSP_Covid21.Models.ViewModel.BrandViewModel>

@{
    var ProductTypeName = Session["ProductTypeName"];
}
<div class="aside">
    <h3 class="aside-title">Brand</h3>
    <div class="checkbox-filter">

        @foreach (var item in Model)
        {
            <div class="input-checkbox">
                <input type="checkbox" id="@item.BrandId" class="brandName_Categories" value="@item.BrandName">
                <label for="@item.BrandId">
                    <span></span>
                    @item.BrandName
                    <small>@item.Product.Count(c => c.PRODUCTTYPE.PRODUCTTYPENAME == ProductTypeName.ToString())</small>
                </label>
            </div>
        }
    </div>
</div>